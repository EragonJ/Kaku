define(function(require) {

  var React = require('react');
  var ToolbarEnlargeButton = React.createClass({
    render: function() {
      <button className="toolbar-enlarge-button">
      </button>
    }
  });

  return ToolbarEnlargeButton;
});
