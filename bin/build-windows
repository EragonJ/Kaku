#!/usr/bin/env node

const shell = require('shelljs');

// 32 bit
shell.exec('./node_modules/.bin/build --windows --ia32');
shell.exec('OS=windows32 npm run afterbuild:os');

// 64 bit
shell.exec('./node_modules/.bin/build --windows --x64');
shell.exec('OS=windows npm run afterbuild:os');
